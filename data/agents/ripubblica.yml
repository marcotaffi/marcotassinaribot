agentName: ripubblica
agentDescription: "Scrive il rilancio di un articolo o evento da una fonte esterna (es. semprenews) per apg23"
apiClient: "gpt-response-dev"
instructions:
  - instructions_ripubblica
  - fogliodistile_ripubblica
  - formato_markdown
params:
  tool_choice: "auto"
  model: "gpt-5.4-mini-2026-03-17"
vectoreStores:
  - vs_7rCG39exRfvB9geL5N2hsWwY
toolNames:
  - gestoredate_now_readClock
  - scraper_url_download
  # RIMOSSO il 2026-09-16 (era un TODO aperto dal 2026-09-15): stesso motivo strutturale di
  # cronaca/ufficiostampa/interviste/dossier (vedi il commento nel toolNames di cronaca.yml) —
  # websearch_italia_low è un tool nativo le cui citazioni auto-annotate dall'API (link con
  # "?utm_source=openai") il validatore di allora non riconosceva. Non più solo un rischio
  # teorico: pubblicato in produzione un articolo reale con la stessa citazione ripetuta 3 volte
  # (una per paragrafo) — arrivato fino a wordpress_apg23_post con status "publish" perché anche
  # il validatore di formatta_ai aveva un buco separato (vedi problemiCitazioniRipetute in
  # taffitools, corretto lo stesso giorno). Le due correzioni insieme (tool rimosso qui +
  # validatore esteso) chiudono il caso da entrambi i lati.
  # - websearch_italia_low
