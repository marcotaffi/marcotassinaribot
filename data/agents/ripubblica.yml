agentName: ripubblica
agentDescription: "Scrive il rilancio di un articolo o evento da una fonte esterna (es. semprenews) per apg23"
apiClient: "gpt-response-dev"
instructions:
  - instructions_ripubblica
  - fogliodistile_ripubblica
  - formato_markdown
params:
  tool_choice: "auto"
  model: "gpt-5.4-mini-2026-03-17"
vectoreStores:
  - vs_7rCG39exRfvB9geL5N2hsWwY
toolNames:
  - gestoredate_now_readClock
  - scraper_url_download
  # TODO (2026-09-15): rivedi se serve — rimosso da cronaca/ufficiostampa/interviste/dossier per
  # lo stesso motivo strutturale (vedi commento nel toolNames di cronaca.yml): websearch_italia_low
  # è un tool nativo il cui risultato non resta disponibile ai giri successivi dello stesso agente
  # (verificato nel codice, non solo un'ipotesi), quindi materiale trovato così può sparire prima
  # di finire nel testo scritto. Non ancora rivisto qui: questo agente scrive un rilancio (di
  # solito da un link già dato), quindi il rischio concreto è probabilmente simile — non l'ho
  # toccato oggi solo perché fuori dall'ambito della richiesta che ha portato agli altri fix.
  - websearch_italia_low
